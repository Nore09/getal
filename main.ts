let getal = 0
basic.forever(function () {
    if (getal < 12) {
        basic.showNumber(0)
        basic.showNumber(3)
        basic.showNumber(6)
        basic.showNumber(9)
        basic.showNumber(12)
    } else {
        getal += 0
        getal += 3
        getal += 6
        getal += 9
        getal += 12
    }
})
